#pragma once
#include<hal-wrapper/device/IDevice.h>

namespace hal
{
	/// <summary>
	///		独立按键。
	///		所谓独立按键就是一个按键占用一个 GPIO 引脚。
	/// </summary>
	class Key :public IDevice
	{
	public:

	};
}
